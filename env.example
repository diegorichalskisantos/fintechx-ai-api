# Exemplo de configuração .env para o projeto FintechX

# Banco de dados MySQL (usado no container docker-compose)
DB_HOST=db
DB_USER=root
DB_PASSWORD=123456
DB_NAME=northwind

# Chave da API OpenAI (substitua por uma real)
OPENAI_API_KEY=sk-SEU_TOKEN_AQUI
